# Briefs de design

Un fichier par ticket qui touche un écran, nommé `<numéro>-<slug>.md`, écrit par l'agent
`ui-designer` avant le code, au format `shape` d'Impeccable (voir `.claude/agents/ui-designer.md`).
Le développeur implémente le brief ; le relecteur compare le résultat au brief.
