---
name: Astreinte SP
description: Le registre de garde de la caserne, porté à l'écran — structure imprimée, accent indigo, états lisibles au soleil et avec des gants.
colors:
  # — Rôles Material 3, thème clair
  primary: "#7655FA"
  on-primary: "#FFFFFF"
  primary-container: "#E4DDFE"
  on-primary-container: "#4D37A2"
  accent-texte: "#5840BC"
  secondary: "#097C69"
  on-secondary: "#FFFFFF"
  secondary-container: "#CEE5E1"
  on-secondary-container: "#065144"
  tertiary: "#9F6224"
  on-tertiary: "#FFFFFF"
  tertiary-container: "#FDEAD7"
  on-tertiary-container: "#131C23"
  orange-vif: "#F59638"
  error: "#BB285D"
  on-error: "#FFFFFF"
  error-container: "#FED7E5"
  on-error-container: "#131C23"
  rose-vif: "#F9357C"
  accent-decoratif: "#B142E8"
  accent-decoratif-fond: "#EFD9FA"
  surface: "#FFFFFF"
  on-surface: "#131C23"
  on-surface-variant: "#45535C"
  surface-dim: "#E7ECEE"
  surface-container-lowest: "#FFFFFF"
  surface-container-low: "#F7F9FA"
  surface-container: "#F1F4F6"
  surface-container-high: "#E9EDF0"
  surface-container-highest: "#E2E7EA"
  outline: "#6E7D87"
  outline-variant: "#C3CDD3"
  inverse-surface: "#222C33"
  on-inverse-surface: "#EDF1F3"
  inverse-primary: "#A690FC"
  shadow: "#0B141A"
  # — Rôles Material 3, thème sombre
  dark-primary: "#A690FC"
  dark-on-primary: "#131C23"
  dark-primary-container: "#3A2B73"
  dark-on-primary-container: "#E4DDFE"
  dark-accent-texte: "#A690FC"
  dark-secondary: "#5FAA9E"
  dark-on-secondary: "#131C23"
  dark-secondary-container: "#14423B"
  dark-on-secondary-container: "#CDE8E2"
  dark-tertiary: "#F59638"
  dark-on-tertiary: "#131C23"
  dark-tertiary-container: "#613305"
  dark-on-tertiary-container: "#FDEBD8"
  dark-error: "#FB7CAA"
  dark-on-error: "#131C23"
  dark-error-container: "#6F203D"
  dark-on-error-container: "#FED7E5"
  dark-accent-decoratif: "#CC84F0"
  dark-accent-decoratif-fond: "#522768"
  dark-surface: "#0F161B"
  dark-on-surface: "#E2E8EC"
  dark-on-surface-variant: "#B3C0C8"
  dark-surface-dim: "#0A1015"
  dark-surface-container-lowest: "#0A1015"
  dark-surface-container-low: "#141C22"
  dark-surface-container: "#182127"
  dark-surface-container-high: "#222C33"
  dark-surface-container-highest: "#2C373E"
  dark-outline: "#7E8D96"
  dark-outline-variant: "#3A454C"
  dark-inverse-surface: "#E2E8EC"
  dark-on-inverse-surface: "#131C23"
  dark-inverse-primary: "#5840BC"
  dark-shadow: "#000000"
  # — Encres d'état (clair)
  etat-disponible: "#5840BC"
  etat-disponible-plein: "#7655FA"
  etat-disponible-fond: "#E4DDFE"
  etat-disponible-sur-fond: "#131C23"
  etat-absent: "#BB285D"
  etat-absent-fond: "#FED7E5"
  etat-absent-sur-fond: "#131C23"
  etat-non-saisi: "#52626C"
  etat-non-saisi-filet: "#73828B"
  etat-attente: "#9F6224"
  etat-attente-fond: "#FDEAD7"
  etat-attente-sur-fond: "#131C23"
  etat-neutre: "#45535C"
  etat-neutre-fond: "#E7EBEE"
  etat-info: "#086959"
  etat-info-fond: "#CEE5E1"
  etat-info-sur-fond: "#065144"
  etat-accepte: "#086959"
  etat-accepte-fond: "#CEE5E1"
  etat-accepte-sur-fond: "#065144"
  etat-annule: "#2A343A"
  etat-archive: "#4A5860"
  etat-archive-fond: "#EEF1F3"
  etat-verrouille: "#3A464E"
  etat-verrouille-fond: "#DDE3E7"
  # — Encres d'état (sombre)
  dark-etat-disponible: "#A690FC"
  dark-etat-disponible-plein: "#5840BC"
  dark-etat-disponible-fond: "#3A2B73"
  dark-etat-disponible-sur-fond: "#E4DDFE"
  dark-etat-absent: "#FB7CAA"
  dark-etat-absent-filet: "#FB7CAA"
  dark-etat-absent-fond: "#6F203D"
  dark-etat-absent-sur-fond: "#FED7E5"
  dark-etat-non-saisi: "#A4B1B9"
  dark-etat-non-saisi-filet: "#6E7D87"
  dark-etat-attente: "#F59638"
  dark-etat-attente-fond: "#613305"
  dark-etat-attente-sur-fond: "#FDEBD8"
  dark-etat-neutre: "#C4CFD6"
  dark-etat-neutre-fond: "#222C33"
  dark-etat-info: "#5FAA9E"
  dark-etat-info-fond: "#14423B"
  dark-etat-info-sur-fond: "#CDE8E2"
  dark-etat-accepte: "#5FAA9E"
  dark-etat-accepte-fond: "#14423B"
  dark-etat-accepte-sur-fond: "#CDE8E2"
  dark-etat-archive: "#A4B1B9"
  dark-etat-archive-fond: "#182127"
  dark-etat-verrouille: "#B3C0C8"
  dark-etat-verrouille-fond: "#222C33"
typography:
  display-nombre:
    fontFamily: "Atkinson Hyperlegible Mono, ui-monospace, monospace"
    fontSize: "32px"
    fontWeight: 700
    lineHeight: 1.13
    letterSpacing: "-0.01em"
  titre-ecran:
    fontFamily: "Archivo, Atkinson Hyperlegible Next, system-ui, sans-serif"
    fontSize: "28px"
    fontWeight: 700
    lineHeight: 1.29
    letterSpacing: "-0.02em"
  titre-section:
    fontFamily: "Archivo, Atkinson Hyperlegible Next, system-ui, sans-serif"
    fontSize: "22px"
    fontWeight: 700
    lineHeight: 1.27
    letterSpacing: "-0.02em"
  titre-bloc:
    fontFamily: "Archivo, Atkinson Hyperlegible Next, system-ui, sans-serif"
    fontSize: "18px"
    fontWeight: 600
    lineHeight: 1.33
    letterSpacing: "-0.01em"
  corps:
    fontFamily: "Atkinson Hyperlegible Next, system-ui, sans-serif"
    fontSize: "16px"
    fontWeight: 400
    lineHeight: 1.5
    letterSpacing: "normal"
  corps-secondaire:
    fontFamily: "Atkinson Hyperlegible Next, system-ui, sans-serif"
    fontSize: "14px"
    fontWeight: 400
    lineHeight: 1.43
    letterSpacing: "normal"
  mention:
    fontFamily: "Atkinson Hyperlegible Next, system-ui, sans-serif"
    fontSize: "13px"
    fontWeight: 400
    lineHeight: 1.38
    letterSpacing: "0.01em"
  libelle-action:
    fontFamily: "Atkinson Hyperlegible Next, system-ui, sans-serif"
    fontSize: "16px"
    fontWeight: 600
    lineHeight: 1.25
    letterSpacing: "0.01em"
  libelle-champ:
    fontFamily: "Atkinson Hyperlegible Next, system-ui, sans-serif"
    fontSize: "14px"
    fontWeight: 600
    lineHeight: 1.29
    letterSpacing: "0.01em"
  etiquette:
    fontFamily: "Atkinson Hyperlegible Next, system-ui, sans-serif"
    fontSize: "12px"
    fontWeight: 700
    lineHeight: 1.33
    letterSpacing: "0.04em"
  nombre:
    fontFamily: "Atkinson Hyperlegible Mono, ui-monospace, monospace"
    fontSize: "20px"
    fontWeight: 600
    lineHeight: 1.2
    letterSpacing: "normal"
  nombre-petit:
    fontFamily: "Atkinson Hyperlegible Mono, ui-monospace, monospace"
    fontSize: "15px"
    fontWeight: 600
    lineHeight: 1.2
    letterSpacing: "normal"
rounded:
  filet: "0px"
  case: "4px"
  controle: "8px"
  feuille: "12px"
  pastille: "999px"
spacing:
  xxs: "2px"
  xs: "4px"
  sm: "8px"
  md: "12px"
  lg: "16px"
  xl: "24px"
  xxl: "32px"
  xxxl: "48px"
components:
  bouton-primaire:
    backgroundColor: "{colors.primary}"
    textColor: "{colors.on-primary}"
    typography: "{typography.libelle-action}"
    rounded: "{rounded.controle}"
    padding: "14px 24px"
    height: "52px"
  bouton-primaire-desactive:
    backgroundColor: "{colors.surface-container-highest}"
    textColor: "{colors.outline}"
    rounded: "{rounded.controle}"
    height: "52px"
  bouton-secondaire:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.accent-texte}"
    typography: "{typography.libelle-action}"
    rounded: "{rounded.controle}"
    padding: "14px 24px"
    height: "52px"
  bouton-danger:
    backgroundColor: "{colors.error}"
    textColor: "{colors.on-error}"
    typography: "{typography.libelle-action}"
    rounded: "{rounded.controle}"
    height: "52px"
  case-disponible:
    backgroundColor: "{colors.etat-disponible-plein}"
    textColor: "{colors.on-primary}"
    rounded: "{rounded.case}"
    height: "48px"
  case-absent:
    backgroundColor: "{colors.etat-absent-fond}"
    textColor: "{colors.etat-absent-sur-fond}"
    rounded: "{rounded.case}"
    height: "48px"
  case-non-saisi:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.etat-non-saisi}"
    rounded: "{rounded.case}"
    height: "48px"
  case-dense:
    rounded: "{rounded.case}"
    height: "28px"
    width: "28px"
  badge-etat:
    backgroundColor: "{colors.etat-neutre-fond}"
    textColor: "{colors.etat-neutre}"
    typography: "{typography.libelle-champ}"
    rounded: "{rounded.case}"
    padding: "4px 8px"
    height: "28px"
  champ-texte:
    backgroundColor: "{colors.surface-container-low}"
    textColor: "{colors.on-surface}"
    typography: "{typography.corps}"
    rounded: "{rounded.controle}"
    padding: "14px 16px"
    height: "56px"
  barre-navigation:
    backgroundColor: "{colors.surface-container-low}"
    textColor: "{colors.on-surface-variant}"
    typography: "{typography.etiquette}"
    height: "72px"
---


# Design System: Astreinte SP

## Overview

**Le monde visuel : le registre de garde.**

Toute caserne tient un registre. Des colonnes réglées, une ligne par entrée, des cases cochées,
une date en marge, rien qu'on efface — on barre et on ajoute une ligne. C'est l'objet que
l'application remplace, et c'est la grammaire graphique que l'application emprunte : la structure
est **imprimée** (filets, colonnes, cases), pas **empilée** (cartes, ombres, pastilles).

Ce que cette direction refuse explicitement :

- Le rouge pompier, le gyrophare, le casque : le propriétaire a confirmé qu'aucun code visuel des
  sapeurs-pompiers n'est imposé, et le rouge est un signal trop précieux pour être dépensé en
  décor. Ici, le rouge veut dire « absent, refusé, cassé », jamais « pompier ».
- Le tableau de bord SaaS bleu à cartes ombrées : c'est la mise en page par défaut de la catégorie,
  et elle gaspille la moitié de l'écran en marges de cartes alors que l'écran clé du produit est
  une grille dense de 62 cases.
- Le violet Material par défaut (`#6750A4`) : un thème Flutter non configuré se reconnaît à ça.

**La thèse.** L'accent de l'application est **l'indigo `#7655FA`** : le bouton principal est un bloc indigo à glyphe blanc (4,72:1), la sélection une pastille indigo pâle, et l'encre reste le texte, les titres et les chiffres. Conséquence voulue : **une couleur saturée dit un état ou un choix, jamais un décor** : indigo pour disponible et choisi, vert pour accepté, orange pour l'attente, rose pour le refus.

**La scène d'usage décide du reste.** Dehors, en plein soleil, souvent avec des gants, parfois à
3 h du matin dans la salle de garde. D'où : thème clair par défaut (le soleil gagne contre un fond
sombre), thème sombre complet pour la nuit, contraste texte ≥ 4.5:1 partout, cibles 48 dp sur
téléphone, texte de base 16 sp, et **chaque état porté par trois signaux redondants — une marque
(forme et remplissage), une icône, un libellé — la couleur n'étant que le quatrième.**

**La preuve de la direction, en une image :** la case du registre. Cochée pleine = disponible.
Hachurée et bordée = absent. Vide avec un filet tireté = non saisi. Ces trois cases restent
distinguables photocopiées en noir et blanc, parce qu'elles diffèrent par le remplissage et la
texture avant de différer par la teinte.

Statut de ce document : il est **normatif et antérieur au code** (convention de ce projet, voir
`CLAUDE.md`). `flutter-dev` l'implémente tel quel au ticket 004 ; tout écart doit être documenté
dans la PR et reporté ici.

## Colors

Stratégie : **Restrained**, neutres plus un accent, et l'accent est désormais l'indigo. Mode Impeccable : Operate. Monde visuel de remplacement décidé au ticket 061, à partir d'une référence et d'une charte fournies par le propriétaire (Archivo, indigo, vert, orange, rose, violet). La couleur reste un porteur d'information, jamais un décor, et aucun état n'est porté par la couleur seule : icône et libellé partout.

### Primary — l'indigo, l'accent
`primary #7655FA` sur `on-primary #FFFFFF` → **4,72:1**. Boutons principaux, élément de navigation choisi, focus, jour courant, filet de sélection, case « disponible » pleine. `primary-container #E4DDFE` sur `on-primary-container #4D37A2` → **6,74:1** : la pastille de sélection de tout composant Material (barre et rail de navigation, tiroir, boutons segmentés, puces). En texte sur blanc, l'indigo vif est trop juste sur le papier : le texte indigo utilise `accent-texte #5840BC` → 7,31:1 sur blanc, 6,92:1 sur le papier `#F7F9FA`. L'encre `#131C23` reste la couleur du texte, des titres et des chiffres ; elle n'est plus celle des boutons.
En sombre : `dark-primary #A690FC` sur `dark-surface #0F161B` → 6,97:1, sur `dark-on-primary #131C23` → 6,58:1 ; `dark-primary-container #3A2B73` sur `dark-on-primary-container #E4DDFE` → 9,07:1.

### Secondary — le vert, l'accepté
`secondary #097C69` sur blanc → **5,12:1**. Accepté, validé, publié, période ouverte, couvert, information. Texte vert sur blanc : `etat-accepte #086959` → 6,61:1. `secondary-container #CEE5E1` sur `on-secondary-container #065144` → 7,02:1.
En sombre : `dark-secondary #5FAA9E` → 6,72:1 ; `dark-secondary-container #14423B` sur `#CDE8E2` → 8,67:1.
Une mesure à connaître : `primary-container` et `etat-accepte-fond` ont la même luminance (1,01:1 entre eux). En niveaux de gris, « choisi » et « accepté » sont le même gris ; l'icône et le libellé les séparent, jamais la teinte. C'est écrit et testé.

### Tertiary — l'orange, l'attente
L'orange de la charte, `orange-vif #F59638`, ne fait que 2,26:1 sur blanc : il ne sert **jamais** de texte, seulement de remplissage de bloc ou de barre. En texte, l'orange est assombri : `tertiary #9F6224` → 4,94:1 sur blanc. `tertiary-container #FDEAD7` sur `on-tertiary-container #131C23` → 14,72:1. Proposé, à pourvoir, en attente de réponse, bannière hors ligne et attention.
En sombre, l'orange vif redevient du texte : `dark-tertiary #F59638` → 8,08:1 ; `dark-tertiary-container #613305` sur `#FDEBD8` → 9,10:1.

### Error — le rose, le refus
`rose-vif #F9357C` fait 3,60:1 sur blanc : remplissage seulement. En texte, `error #BB285D` → 5,87:1. `error-container #FED7E5` sur `on-error-container #131C23` → 13,19:1. Refusé, absent, conflit, erreur.
En sombre : `dark-error #FB7CAA` → 7,44:1 ; `dark-error-container #6F203D` sur `#FED7E5` → 8,24:1.

### Accent décoratif — le violet
`accent-decoratif #B142E8` (4,36:1 sur blanc) et `accent-decoratif-fond #EFD9FA`. Marque, illustration d'état vide, rien d'autre : jamais du texte, jamais un état. En sombre `dark-accent-decoratif #CC84F0`, fond `#522768`. Un test refuse que ce token, `orange-vif` ou `rose-vif` atteignent le seuil du texte : tant qu'ils échouent à 4,5:1, personne ne peut les glisser dans un style de texte en croyant bien faire.

### Neutral — le papier et l'encre
Inchangés depuis le ticket 004 : `surface #FFFFFF`, `on-surface #131C23`, `on-surface-variant #45535C`, les crans de surface `#F7F9FA`, `#F1F4F6`, `#E9EDF0`, `#E2E7EA`, `outline #6E7D87`, `outline-variant #C3CDD3`, et leurs équivalents sombres.

### Named Rules — la sémantique des états
- **Disponible** : famille indigo. Texte `etat-disponible #5840BC`, case pleine `etat-disponible-plein #7655FA` (4,72:1 sous un glyphe blanc, le ratio le plus bas des paires de texte du système clair), fond `etat-disponible-fond #E4DDFE`, texte sur fond encre.
- **Accepté, validé, publié, période ouverte, couvert, information** : famille verte. Texte `etat-accepte #086959`, fond `etat-accepte-fond #CEE5E1`, texte sur fond `#065144`.
- **Proposé, à pourvoir, en attente** : famille orange. Texte `etat-attente #9F6224`, fond `etat-attente-fond #FDEAD7`, texte sur fond encre.
- **Absent, refusé, conflit** : famille rose. Texte `etat-absent #BB285D`, fond `etat-absent-fond #FED7E5`, texte sur fond encre.
- **Non saisi, neutre, annulé, archivé, verrouillé** : gris inchangés.
- **Sélection** : `primary-container` sur `on-primary-container`, pour tout composant Material qui exprime un choix, réglé dans `app_theme.dart`. Le vert ne dit jamais « choisi ».
- Texte coloré sur blanc : indigo `accent-texte`, vert `etat-accepte`, et les deux teintes assombries `tertiary #9F6224` (4,94:1) et `error #BB285D` (5,87:1) pour un libellé d'état qui doit être lu. Les teintes vives `orange-vif`, `rose-vif` et `accent-decoratif`, jamais : un test les y refuse (§ Écarts, ticket 061).

#### Disponibilité — la grammaire de la case

| État | Marque | Icône | Libellé | Encre clair | Encre sombre |
|---|---|---|---|---|---|
| **disponible** | case pleine | `Icons.check_box` (`check` dans la grille) | « Disponible » | fond `#7655FA`, glyphe `#FFFFFF` (4.72:1) | fond `#5840BC`, glyphe `#FFFFFF` (7.31:1) |
| **absent** | case hachurée à 45°, bord 2 dp | `Icons.disabled_by_default` (`close` dans la grille) | « Absent » | fond `#FED7E5`, bord et hachures `#BB285D` (4,49:1 sur son fond #FED7E5, 5,87:1 sur blanc) | fond `#6F203D`, bord `#FB7CAA` (4.39:1) |
| **non saisi** | case vide, filet tireté 3/3 | `Icons.check_box_outline_blank` (`remove` dans la grille) | « Non saisi » | fond `surface`, filet `#73828B`, glyphe `#52626C` (6.32:1) | filet `#6E7D87`, glyphe `#A4B1B9` (8.31:1) |

Le triplet vient du registre lui-même : cochée / barrée / vide. Il tient en niveaux de gris
(luminance relative 0.17 / 0.75 hachurée de 0.13 / 1.00) et il tient à 40 cm au soleil.

#### Créneau — jour et nuit

Le créneau **ne porte aucune teinte**. Il est signé par trois choses :

| Créneau | Fond | Icône | Libellé | Position en grille |
|---|---|---|---|---|
| **jour** | `surface` (clair) | `Icons.light_mode` | « Jour » | moitié haute de la case journée |
| **nuit** | `surface-container-high` (un cran plus sombre) | `Icons.bedtime` | « Nuit » | moitié basse |

L'écart de valeur jour/nuit est volontairement faible (1.18:1) : c'est un repère de lecture, pas un
état. Ce qui distingue le jour de la nuit est l'icône et la position ; le fond ne fait que confirmer.

#### Attribution

| État | Marque | Icône | Libellé | Encre clair | Encre sombre |
|---|---|---|---|---|---|
| **proposé** | contour 2 dp, fond ocre pâle | `Icons.hourglass_top` | « En attente de ta réponse » / « En attente » | `#131C23` sur `#FDEAD7` (14.72:1) | `#FDEBD8` sur `#613305` (9.10:1) |
| **accepté** | plein, tampon | `Icons.task_alt` | « Accepté » | `#065144` sur `#CEE5E1` (7.02:1) | `#CDE8E2` sur `#14423B` (8.67:1) |
| **refusé** | barré | `Icons.cancel` | « Refusé » | `#131C23` sur `#FED7E5` (13.19:1) | `#FED7E5` sur `#6F203D` (8.24:1) |
| **annulé** | barré, atténué | `Icons.block` | « Annulé » | `#2A343A` sur `#E7EBEE` (10.61:1) | `#C4CFD6` sur `#222C33` (8.97:1) |

`accepté` est vert, `disponible` est indigo : deux familles distinctes, et l'icône plus le libellé les séparent en niveaux de gris. `refusé` et `absent` partagent le rose `#BB285D` mais jamais l'icône (`cancel` ≠
`disabled_by_default`). « Annulé » ne se supprime pas : il se barre et reste lisible (principe
produit 5).

#### Planning

| État | Marque | Icône | Libellé | Encre clair | Encre sombre |
|---|---|---|---|---|---|
| **brouillon** | contour tireté | `Icons.edit_note` | « Brouillon » | `#45535C` sur `#E7EBEE` (6.62:1) | `#C4CFD6` sur `#222C33` (8.97:1) |
| **publié** | plein, filet vert | `Icons.campaign` | « Publié » | `#065144` sur `#CEE5E1` (7.02:1) | `#CDE8E2` sur `#14423B` (8.67:1) |
| **validé** | tampon | `Icons.verified` | « Validé » | `#065144` sur `#CEE5E1` (7.02:1) | `#CDE8E2` sur `#14423B` (8.67:1) |
| **archivé** | atténué | `Icons.inventory_2` | « Archivé » | `#4A5860` sur `#EEF1F3` (6.48:1) | `#A4B1B9` sur `#182127` (7.44:1) |

#### Période de saisie

| État | Marque | Icône | Libellé | Encre clair | Encre sombre |
|---|---|---|---|---|---|
| **ouverte** | filet vert en marge | `Icons.lock_open` | « Saisie ouverte jusqu'au {date} » | `#065144` sur `#CEE5E1` (7.02:1) | `#CDE8E2` sur `#14423B` (8.67:1) |
| **verrouillée** | fond hachuré gris, cases non interactives | `Icons.lock` | « Mois verrouillé » | `#3A464E` sur `#DDE3E7` (7.49:1) | `#B3C0C8` sur `#222C33` (7.65:1) |

Le verrouillage **n'est pas une erreur** : il est gris-encre, jamais rouge. C'est un fait, pas une
panne. Les cases restent lisibles, seule l'interaction disparaît, et le motif hachuré couvre la
grille entière pour que l'état soit compris sans lire la bannière.

#### Hors ligne et synchronisation

| État | Icône | Libellé | Encre clair | Encre sombre |
|---|---|---|---|---|
| **au repos** | `Icons.cloud_done_outlined` | « À jour » | `on-surface-variant` sur `surface` (7.94:1) | `#B3C0C8` sur `#0F161B` (9.81:1) |
| **hors ligne** | `Icons.cloud_off` | « Hors ligne — tes modifications partiront au retour du réseau » | encre sur fond orange `#131C23` sur `#FDEAD7` (14.72:1) | `#FDEBD8` sur `#613305` (9.10:1) |
| **enregistrement** | `Icons.sync` | « Enregistrement… » | `on-surface-variant` sur `surface` | `#B3C0C8` sur `#0F161B` |
| **enregistré** | `Icons.cloud_done` | « Enregistré » | `#086959` sur `surface` (6.61:1) | `#5FAA9E` sur `#0F161B` (6.72:1) |
| **échec** | `Icons.error_outline` | « Non enregistré » + action « Réessayer » | `#BB285D` sur `surface` (5.87:1) | `#FB7CAA` sur `#0F161B` (7.44:1) |

Un cinquième état, **au repos**, a été ajouté à l'implémentation : `SaveIndicator` doit pouvoir
exister sur un écran où rien n'est en cours, sans disparaître ni mentir. Les encres sombres de cette
famille, de « planning » et de « période » réemploient des tokens déjà listés dans l'en-tête de ce
document ; aucune valeur nouvelle n'a été inventée.

## Typography

**Trois fontes, chacune à sa place, décidées au ticket 061.** **Archivo** (SIL OFL, embarquée dans `assets/fonts/`, graisses 600 et 700) porte les trois styles de titre à 28, 22 et 18 points, avec un interlettrage au plus de −0,02 em : c'est là que se voit le caractère de la référence choisie par le propriétaire, et là où la confusion de glyphes n'a pas d'enjeu. **Atkinson Hyperlegible Next** garde tout le texte à 16 points et moins, pour la raison de brief du ticket 004 : distinction `1`/`l`/`I` et `0`/`O`, lecture au soleil et en mouvement, public de tous âges. **Atkinson Hyperlegible Mono** garde tous les chiffres.

**Un second cut pour les nombres : Atkinson Hyperlegible Mono**, même famille, chasse fixe. Tout
nombre qui s'aligne verticalement ou qui change en place — compteurs jour/nuit/weekend, quotas
« 2/3 », en-têtes de jours de la matrice, code OTP, dates — le prend. C'est de la mesure, pas un
costume technique.

- **Les trois fontes sont embarquées dans `assets/fonts/`**, jamais chargées depuis un CDN : la PWA
  doit s'ouvrir vite en 4G rurale, et un FOUT sur une grille d'états est un défaut de lecture.
- Si une fonte manque, repli explicite : même famille + `FontFeature.tabularFigures()`
  sur tous les styles numériques, écart à signaler dans la PR. **Ce repli n'a pas été nécessaire :
  les trois familles sont disponibles.**
- `FontFeature.tabularFigures()` est appliqué **en plus** sur les styles `nombre*` dans tous les cas.
- Repli système : `system-ui, -apple-system, Segoe UI, sans-serif`.

**Fichiers retenus aux tickets 004 et 061.** Source : dépôt officiel Google Fonts, `ofl/atkinsonhyperlegiblenext`
et `ofl/atkinsonhyperlegiblemono`, publiés par le Braille Institute sous licence SIL OFL 1.1. Les familles Atkinson n'existent en amont que sous forme **variable** (axe `wght` 200–800) ; Archivo vient de `ofl/archivo` avec son axe de largeur figé. L'application embarque
des **instances statiques sous-ensemblées** au latin et aux diacritiques français, produites avec
`fontTools` (procédure reproductible dans `assets/fonts/README.md`) :

| Fichier | Graisse | Poids |
|---|---|---|
| `AtkinsonHyperlegibleNext-Regular.ttf` | 400 | 43 Ko |
| `AtkinsonHyperlegibleNext-SemiBold.ttf` | 600 | 43 Ko |
| `AtkinsonHyperlegibleNext-Bold.ttf` | 700 | 43 Ko |
| `AtkinsonHyperlegibleMono-SemiBold.ttf` | 600 | 30 Ko |
| `AtkinsonHyperlegibleMono-Bold.ttf` | 700 | 30 Ko |
| `AtkinsonHyperlegibleNext-Repli.ttf` | — | 10 Ko |
| `Archivo-SemiBold.ttf` | 600 | 58 Ko |
| `Archivo-Bold.ttf` | 700 | 58 Ko |
| **Total** | | **316 Ko** |

316 Ko sur disque (315 720 octets), huit fichiers ; 41 Ko de plus au démarrage une fois compressés en brotli. Le budget de 180 Ko du ticket 004 est dépassé sciemment au ticket 061, pour deux graisses d'Archivo sur les titres. `AtkinsonHyperlegibleNext-Repli.ttf` est un repli déclaré sous la famille Roboto pour désamorcer le chargement système. Les italiques ne sont pas embarquées
(l'italique est proscrit comme moyen de hiérarchie) et les graisses 200, 300, 500 et 800 non plus
(l'échelle n'utilise que 400, 600 et 700).

**Pourquoi du TTF et pas du WOFF2.** Essayé et mesuré au ticket 004 : les cinq fichiers convertis
en WOFF2 pèsent 72 Ko au lieu de 184 Ko, mais **Flutter web ne sait pas les décoder**. Le moteur
passe les octets de la police à Skia, dont le gestionnaire de fontes ne lit que du `sfnt` (TTF,
OTF) ; à l'exécution, les cinq WOFF2 se téléchargent bien (200), échouent silencieusement au
décodage, et l'application **retombe sur un Roboto téléchargé depuis `fonts.gstatic.com`** —
exactement la dépendance réseau et le FOUT que ce document interdit. Vérifié au navigateur, capture
et journal réseau à l'appui.

Le bon levier est donc la compression de transport, que tout hébergeur statique applique :

| Format | Poids sur le fil |
|---|---|
| TTF brut | 184 Ko |
| TTF + gzip | 92 Ko |
| **TTF + brotli** | **83 Ko** |
| WOFF2 (inutilisable ici) | 72 Ko |

Brotli sur le TTF arrive à 11 Ko du WOFF2. L'exigence opérationnelle est donc : **servir
`assets/fonts/*.ttf` avec `Content-Encoding: br`**, pas convertir les fichiers.

### Hierarchy

Échelle fixe en `rem`/`sp`, ratio ≈ 1.2. Pas de typographie fluide : l'utilisateur lit à DPI
constant et un titre qui rétrécit dans un panneau est pire, pas mieux.

| Token | Rôle Material 3 | Taille / interligne / graisse | Emploi |
|---|---|---|---|
| `display-nombre` | `displaySmall` | 32 / 36 / 700 mono | compteur du mois, gros total |
| `titre-ecran` | `headlineMedium` | 28 / 36 / 700 | titre d'écran (« Octobre 2026 ») |
| `titre-section` | `titleLarge` | 22 / 28 / 700 | barre d'application, titre de feuille |
| `titre-bloc` | `titleMedium` | 18 / 24 / 600 | en-tête de groupe, nom de membre |
| `corps` | `bodyLarge` | 16 / 24 / 400 | **texte de base, jamais plus petit pour une info nécessaire** |
| `corps-secondaire` | `bodyMedium` | 14 / 20 / 400 | texte de soutien |
| `mention` | `bodySmall` | 13 / 18 / 400 | horodatage, note de bas de carte |
| `libelle-action` | `labelLarge` | 16 / 20 / 600 | **libellé de bouton — 16, pas 14** |
| `libelle-champ` | `labelMedium` | 14 / 18 / 600 | libellé de champ, badge d'état |
| `etiquette` | `labelSmall` | 12 / 16 / 700, +0.04em | en-tête de colonne, libellé de navigation |
| `nombre` | — | 20 / 24 / 600 mono | quota, compteur en ligne |
| `nombre-petit` | — | 15 / 18 / 600 mono | numéro du jour dans la grille, matrice |

### Named Rules

- **Tailles dynamiques respectées.** Aucun `textScaler` bridé en dessous de 1.0. Au-delà de **1.6**,
  la grille mensuelle et la matrice admin **changent de forme** (passage en liste de jours) plutôt
  que de rogner le texte. Bornage à 2.0 uniquement à l'intérieur de la matrice dense.
- **Jamais de texte en dessous de 12 sp**, et jamais d'information nécessaire en dessous de 14 sp.
- Mesure de lecture 65–75 caractères pour la prose (aide, RGPD, message d'erreur long) ; les
  tableaux et grilles n'y sont pas soumis.
- Pas de graisse sous 400. Le gras (600/700) est le seul moyen d'emphase avec la taille ; pas
  d'italique pour hiérarchiser, pas de MAJUSCULES sauf `etiquette`.
- Trois fontes, une ligne de partage : 18 points et au-dessus en Archivo, 16 points et en dessous en Atkinson Hyperlegible Next, tout chiffre en Atkinson Hyperlegible Mono. Aucune autre fonte, nulle part, y compris sur les écrans de connexion.

## Layout

### Espacement

Échelle de 4 : `2, 4, 8, 12, 16, 24, 32, 48`. Rythme unique dans toute l'app.

- Marge de page : **16** (compact), **24** (medium / expanded), **32** (large).
- Écart entre cases de la grille : **4** en compact, **6** en expanded, **2** en matrice dense.
- Écart entre groupes : **24**. À l'intérieur d'un groupe : **8**.
- Toujours plus d'espace **au-dessus** d'un titre qu'en dessous (24 / 8).
- Écart minimal entre deux cibles tactiles adjacentes : **8**.

### Cibles tactiles

| Contexte | Taille minimale |
|---|---|
| Téléphone, tout contrôle | **48 × 48 dp** (le plancher est 44, on vise 48 : gants) |
| Case de créneau, grille mensuelle | **48 × 44 dp** minimum |
| Bouton principal | hauteur **52 dp**, pleine largeur sur téléphone |
| Matrice admin, **pointeur uniquement** | **28 × 28 px**, écart 2 px, jamais servie au tactile |

La densité dense n'est jamais activée sur un pointeur grossier (`MediaQuery.of(context).navigationMode`
/ largeur compacte) : sur tablette tactile, la matrice reste en densité compacte (40 dp) et défile.

### Points de rupture

Classes de fenêtre Material 3, seuils sur la **largeur** en dp.

| Classe | Largeur | Cible | Navigation | Composition |
|---|---|---|---|---|
| **compact** | < 600 | téléphone, PWA installée | `NavigationBar` en bas, libellés toujours visibles | 1 colonne ; grille mensuelle 7 colonnes × 2 créneaux ; détail en feuille de bas d'écran |
| **medium** | 600–839 | tablette portrait, téléphone paysage | `NavigationRail` (icônes + libellés) | 1 colonne de contenu ≤ 720 ; grille plus aérée ; feuille latérale possible |
| **expanded** | 840–1199 | tablette paysage, petit portable | `NavigationRail` étendu | 2 volets : liste + détail. Matrice admin avec **colonne « membre » figée** et en-tête de dates collant |
| **large** | ≥ 1200 | poste admin (écran principal du ticket 016) | `NavigationRail` étendu permanent | 3 zones : navigation, matrice pleine densité, panneau de détail du créneau à droite (**pas de modale**). Contenu centré, max 1440 |

Conséquences non négociables :

- La matrice admin est conçue **d'abord pour large**, puis dégradée. Sur compact elle est consultable
  (défilement horizontal, colonne membre figée), pas éditable en masse.
- La grille mensuelle du membre est conçue **d'abord pour compact**.
- Aucun affichage ne dépend du survol ni du clic droit. Toute action de menu contextuel a un
  équivalent visible (bouton « ⋮ » de 48 dp).
- Le glissement continu de sélection (ticket 011) a toujours un équivalent par touches simples et
  par raccourcis. Pas de geste caché sans équivalent visible.

### Zones sûres et chrome PWA

- La barre de navigation ajoute `MediaQuery.viewPadding.bottom` (barre d'accueil iOS) à sa hauteur ;
  le contenu défilant reçoit un `padding` bas égal à la hauteur de nav + zone sûre.
- En mode `standalone` iOS, la zone haute est gérée par `SafeArea` ; la barre d'application ne
  passe jamais sous l'heure du système.
- Le geste retour iOS reste actif : aucune zone de glissement horizontal sur les 24 premiers dp
  du bord gauche (contrainte à respecter dans la grille à sélection par glissement).
- La couleur de la barre d'état / `theme-color` du manifeste suit `surface-container-low` du thème
  actif.

## Elevation & Depth

**La profondeur n'est pas le matériau de ce système : le filet l'est.** La séparation se fait par
un trait de réglure et par un cran de surface tonale, pas par une ombre. Une carte ombrée par défaut
est un défaut de revue.

| Niveau | Emploi | Rendu |
|---|---|---|
| **0** | tout contenu, panneaux, listes, « cartes » | aucune ombre. Filet `outline-variant` 1 dp et/ou surface tonale d'un cran |
| **1** | barre d'application quand le contenu défile dessous, barre de navigation | surface tonale `surface-container-low` + filet 1 dp, pas d'ombre |
| **2** | menu, info-bulle, sélecteur | `0 2 8 rgba(shadow, 0.10)` |
| **3** | feuille de bas d'écran, dialogue | `0 8 24 rgba(shadow, 0.14)` + `scrim` 32 % |
| **4** | snackbar, bouton flottant | `0 4 12 rgba(shadow, 0.12)` |

Toute ombre porte un décalage **et** un flou. Aucune ombre colorée, aucun halo à décalage zéro.
Teinte d'ombre : `#0B141A` en clair, `#000000` en sombre.

### Motion

Le mouvement dit un changement d'état. Rien d'autre.

| Token | Durée | Courbe | Emploi |
|---|---|---|---|
| `instantane` | 120 ms | `easeOutCubic` | case cochée/décochée, bascule |
| `courant` | 180 ms | `easeOutCubic` | badge, bannière, apparition d'un état |
| `surface` | 240 ms | `easeOutCubic` | feuille, panneau, navigation |
| `page` | 280 ms | `easeInOutCubic` | transition de route |

- **Un seul moment chorégraphié dans toute l'application : le tampon.** Quand une proposition passe
  à « Accepté » ou qu'un planning passe à « Validé », le badge apparaît en `scale 1.06 → 1.00` +
  opacité sur 180 ms, une fois. C'est le geste du tampon qui se pose. Nulle part ailleurs.
- **Reduce Motion / `MediaQuery.disableAnimations` est respecté** : toutes les durées tombent à 0,
  le `LoadingSkeleton` perd son balayage et devient un aplat statique, les transitions de page
  deviennent des fondus instantanés. C'est une contrainte produit (PRODUCT.md), pas une option.
- Pas de séquence d'entrée orchestrée au chargement d'écran. L'app s'ouvre sur une tâche.
- Pas d'animation sur les listes longues ni sur la matrice admin.

## Shapes

Le registre est fait de cases, pas de pastilles.

| Token | Rayon | Emploi |
|---|---|---|
| `filet` | 0 | traits de réglure, en-têtes collants, séparateurs |
| `case` | 4 | **case de créneau, badge d'état, puce** — la forme signature |
| `controle` | 8 | bouton, champ de saisie, panneau, « carte » |
| `feuille` | 12 (haut uniquement pour les feuilles de bas d'écran) | dialogue, feuille, menu |
| `pastille` | 999 | avatar, pastille de compteur de notifications uniquement |

Écart assumé au Material 3 par défaut : **pas de boutons en gélule**. Le bouton est un bloc à
rayon 8. La cohérence entre la case à rayon 4 et le bouton à rayon 8 est ce qui donne à l'app son
air de document imprimé plutôt que d'application grand public.

Épaisseurs de trait : réglure **1 dp**, contour de contrôle **1 dp**, contour porteur d'état
(case sélectionnée, case absente, focus) **2 dp**. Anneau de focus : 2 dp `primary` + 2 dp de
décalage, visible sur tous les fonds, jamais supprimé.

Hachures : trait 1.5 dp à 45°, pas de 6 dp, opacité 28 % de l'encre de l'état. Utilisées pour
« absent » et pour « mois verrouillé », nulle part ailleurs.

## Components

### Buttons

`PrimaryButton` couvre les trois variantes, avec les mêmes dimensions.

| Variante | Fond | Texte | Bordure |
|---|---|---|---|
| `primaire` | `primary` | `on-primary` | — |
| `secondaire` | `surface` | `accent-texte` | 1 dp `outline` |
| `danger` | `error` | `on-error` | — |

- États obligatoires : normal, pressé (`primary` + 8 % d'`on-primary` en surimpression), focus
  (anneau 2 dp), **désactivé** (fond `surface-container-highest`, texte `outline`, curseur
  interdit, **et une raison affichée à côté** — un bouton grisé sans explication est un défaut),
  **chargement** (le libellé reste, un indicateur de 20 dp le précède, la largeur ne bouge pas).
- Hauteur 52 dp, pleine largeur sur compact, largeur intrinsèque ≥ 160 dp ailleurs.
- Icône facultative à gauche, 20 dp, même couleur que le texte.
- Le libellé nomme l'action : « Enregistrer le mois », jamais « OK ».

### Chips — `SlotChip`, la case du registre

Le composant signature. Une case = un créneau (date + jour/nuit) + un état de disponibilité.

- Trois densités : `confortable` 48 dp (téléphone), `compacte` 40 dp (tablette, listes),
  `dense` 28 px (**matrice admin, pointeur uniquement**).
- Contenu : la marque d'état centrée (glyphe 24 / 20 / 16 dp selon la densité) ; en densité dense,
  la marque seule.
- États visuels : les trois états de disponibilité ci-dessus, plus `sélectionné` (contour 2 dp
  `primary`), `désactivé/verrouillé` (hachures grises, non focalisable), `en enregistrement`
  (le contour pulse une fois — supprimé si Reduce Motion), `erreur` (contour 2 dp `error`).
- Sémantique d'accessibilité obligatoire : `Semantics(label: "Samedi 4 octobre, nuit, disponible",
  button: true, toggled: …)`. Le libellé est une phrase complète, pas un code.
- Le glissement continu est pris en charge par le parent (`DayCell` / la grille), jamais par la
  case elle-même.

### Cards / Containers

Il n'y a pas de composant « carte ». Il y a des **blocs réglés** : fond `surface`, filet 1 dp
`outline-variant`, rayon 8, sans ombre. Les blocs ne s'imbriquent jamais. Une liste de blocs
identiques faits d'une icône + un titre + un texte est interdite comme structure de page.

`DayCell` est un bloc réglé : numéro du jour en `nombre-petit`, nom du jour en `etiquette`,
marqueur weekend (fond `surface-dim` + nom en gras) et marqueur férié (`Icons.star` 14 dp + nom du
jour férié en info-bulle **et** en semantics), puis deux `SlotChip` empilés jour au-dessus, nuit
en dessous. Le jour courant porte un filet `primary` de 2 dp sur son bord gauche et le libellé
« Aujourd'hui » en semantics.

### Inputs / Fields

Champ rempli Material 3 : fond `surface-container-low`, filet bas 1 dp `outline`, rayon 8,
hauteur 56, texte `corps` 16 sp (jamais moins : en dessous de 16, iOS zoome à la saisie).

- Libellé toujours visible au-dessus du champ, jamais seulement en placeholder.
- Erreur : filet 2 dp `error`, icône `Icons.error_outline`, message sous le champ en `corps-secondaire`
  couleur `error`, **annoncé** (`liveRegion: true`).
- Le message d'erreur nomme le problème **et** la sortie : « Code incorrect. Vérifie les 6 chiffres
  ou demande un nouveau code. »
- `inputmode` / `keyboardType` adapté : `number` pour le code OTP et les quotas, `emailAddress`
  pour l'email, `phone` pour le téléphone.

### Navigation

`AppScaffold` porte la barre d'application, la navigation, les bannières transverses et les zones
sûres. Destinations, dans cet ordre :

| Destination | Icône (sélectionnée / non) | Libellé | Route |
|---|---|---|---|
| Mon mois | `calendar_month` / `calendar_month_outlined` | « Mon mois » | `/mois` |
| Propositions | `inbox` / `inbox_outlined` + pastille de compte | « Propositions » | `/propositions` |
| Planning | `groups` / `groups_outlined` | « Planning » | `/planning` |
| Profil | `person` / `person_outline` | « Profil » | `/profil` |
| Admin *(admins seuls)* | `admin_panel_settings` / `admin_panel_settings_outlined` | « Admin » | `/admin` |

- Libellés **toujours affichés** (`NavigationDestinationLabelBehavior.alwaysShow`) : le public a une
  aisance numérique variable, une icône seule ne suffit pas.
- Maximum 5 destinations, ce qui est exactement le compte pour un admin. Aucune destination ne
  s'ajoute sans en retirer une.
- La pastille « Propositions » affiche le nombre de propositions en attente, plafonné à « 9+ »,
  et double le chiffre d'un libellé de semantics (« 3 propositions en attente »).
- Le bouton retour du navigateur et le geste retour iOS restent fonctionnels : chaque écran est une
  route nommée, jamais un état local.
- L'élément choisi porte `primary-container` et `on-primary-container`, en barre, en rail, en tiroir et en boutons segmentés : la sélection est toujours indigo.

### [Signature Component] — La bannière de contexte, `AppBanner`

Bandeau plein largeur, sous la barre d'application, rayon 0, filet haut et bas 1 dp. Il porte les
faits qui changent tout ce qui est en dessous, et qu'on doit comprendre sans lire :

| Variante | Fond / encre | Icône | Exemple |
|---|---|---|---|
| `verrouille` | `#DDE3E7` / `#3A464E`, fond hachuré | `lock` | « Mois verrouillé depuis le 15 septembre. Contacte ton chef de centre. » |
| `information` | `etat-info-fond` / `etat-info-sur-fond` | `info_outline` | « Saisie ouverte jusqu'au 15 septembre. » |
| `attention` | `etat-attente-fond` / `etat-attente-sur-fond` | `schedule` | « Plus que 2 jours pour saisir octobre. » |
| `hors-ligne` | `etat-attente-fond` / `etat-attente-sur-fond` | `cloud_off` | « Hors ligne. Tes modifications partiront au retour du réseau. » |
| `lecture-seule` | `etat-neutre-fond` / `etat-neutre`, fond hachuré | `visibility` | « Caserne suspendue : lecture seule. » |
| `erreur` | `error-container` / `on-error-container` | `error_outline` | « Impossible d'enregistrer. » + action « Réessayer » |

Une seule bannière à la fois, par ordre de priorité : erreur > hors-ligne > lecture-seule >
verrouillé > attention > information. La bannière n'est jamais fermable si elle décrit un état
persistant.

## Do's and Don'ts

### Do:

- Porter chaque état par **marque + icône + libellé**, la couleur en quatrième. Vérifier chaque
  nouvel état en niveaux de gris avant de l'appeler fini.
- Séparer par un **filet** et un cran de surface. Réserver l'ombre à ce qui flotte vraiment.
- Utiliser les chiffres tabulaires partout où un nombre s'aligne ou change en place.
- Donner 48 dp à tout ce qui se touche, 8 dp d'écart entre deux cibles.
- Nommer l'action dans le bouton et la sortie dans l'erreur.
- Expliquer pourquoi un contrôle est désactivé, à côté du contrôle.
- Tutoyer le membre, vouvoyer personne, faire des phrases courtes.
- Écrire chaque chaîne dans `AppStrings`, jamais dans un widget.
- Traiter « verrouillé », « suspendu », « annulé » comme des faits gris, pas comme des alarmes rouges.
- Régler dans `app_theme.dart` tout composant Material qui exprime une sélection, sinon il retombe sur le vert de Material et dit « accepté » au lieu de « choisi ».

### Don't:

- Pas de rouge décoratif, pas de gyrophare, pas de casque, pas de dégradé de marque. Le rouge est
  un état.
- Pas de violet Material par défaut, pas de bleu SaaS générique comme accent.
- Pas de carte ombrée comme structure de page, pas de carte dans une carte.
- Pas de pastille (`rounded-full`) sur un bouton ou un champ. Le registre a des cases.
- Pas d'emoji en guise d'icône, pas de glyphe Unicode : icônes Material Icons uniquement, un seul
  style, une seule graisse.
- Pas de texte gris clair sur fond gris. Pas de texte nécessaire sous 14 sp.
- Pas de survol ni de clic droit comme seul accès à une action.
- Pas de modale pour une tâche qui ne demande ni interruption ni protection : sur grand écran, le
  détail d'un créneau s'ouvre dans le panneau de droite ; sur téléphone, dans une feuille de bas
  d'écran avec le geste retour.
- Pas de `CircularProgressIndicator` au milieu d'un écran : squelette à la forme du contenu attendu.
- Pas d'état vide muet : un état vide explique et propose une action.
- Pas de séquence d'animation au chargement, pas de mouvement qui ne dit pas un changement d'état.
- Mettre `orange-vif`, `rose-vif` ou `accent-decoratif` dans un style de texte.

## Écarts d'implémentation (ticket 004)

Ce document reste normatif. Les points ci-dessous sont les endroits où le code a dû trancher, avec
la raison ; ils sont désormais **la** référence.

| Point | Ce que disait le document | Ce que fait le code | Pourquoi |
|---|---|---|---|
| Densité `compacte` de `SlotChip` | 40 dp | 40 dp en lecture seule, **44 dp dès que la case est actionnable** | 40 dp passe sous le plancher tactile WCAG. La valeur d'affichage est conservée, la cible remonte au plancher quand il y a quelque chose à toucher. |
| Densité `dense` de `SlotChip` | « jamais servie au tactile » | actionnable **au pointeur fin**, inerte au doigt | La matrice du ticket 016 se clique. Ce qui est interdit à 28 px, c'est le doigt, pas la souris. |
| Glyphe de la case | `check_box` / `disabled_by_default` / `check_box_outline_blank`, et `check` / `close` / `remove` « dans la grille » | `StatusDescriptor` porte les deux : `icone` pour le badge, `iconeCase` pour la case | Deux glyphes par état, un seul objet. Aucun écran ne peut prendre le mauvais. |
| `FocusTheme` | listé parmi les thèmes de composant | n'existe pas dans Flutter | L'anneau de focus passe par `ThemeData.focusColor` et par le `WidgetStateProperty` de chaque thème de composant. |
| `themeMode: ThemeMode.system` | écrit explicitement dans `lib/app.dart` | laissé à la valeur par défaut de `MaterialApp`, qui **est** `ThemeMode.system` | L'écrire est un argument redondant que `flutter analyze` refuse, et `flutter analyze` doit être vierge. |
| Compteur `CountStat` | échelle de texte respectée sans borne | le **nombre** se réduit jusqu'à la largeur disponible (`BoxFit.scaleDown`), le libellé garde sa taille | Au-delà de ×1.6 un gros total déborde sa colonne. Un compteur un peu plus petit vaut mieux qu'un compteur tronqué. |
| `NavigationRail` | rail fixe | rail défilant à hauteur pleine | Cinq destinations ne tiennent pas sur un téléphone en paysage (600 × 360) ni à grande échelle de texte. |
| Fichier `hachures.dart` | absent de la liste des composants | ajouté | Ce n'est pas un composant mais une primitive de dessin, partagée par `SlotChip` et `AppBanner` ; sans elle, une bannière devrait importer une case. |
| `SyncEtat` | quatre états | cinq, avec `repos` | `SaveIndicator` doit exister sur un écran où rien n'est en cours. |
| Écrans métier de remplacement | « chaque destination pointe sur un écran de remplacement neutre » | non livrés | Le ticket ne livre que le socle et `/dev/components` ; les destinations sont démontrées dans le catalogue. Les routes arrivent avec leurs tickets. |

## Écarts d'implémentation (ticket 011)

| Point | Ce que disait le document | Ce que fait le code | Pourquoi |
|---|---|---|---|
| Forme de la grille en `compact` | « grille mensuelle 7 colonnes × 2 créneaux » | **registre à trois colonnes**, une ligne par jour ; le calendrier à sept colonnes reprend dès `expanded` | `7 × 48 + 6 × 8 = 384 dp` contre 328 dp disponibles sur un téléphone de 360. La contrainte tactile gagne (`design/011 § 3`). |
| Icône de créneau par case | « moitié haute / moitié basse », une icône par case | en orientation `ligne`, l'icône vit dans l'en-tête de colonnes épinglé | La position en colonne et l'en-tête épinglé portent la distinction ; soixante-deux icônes répétées sont du bruit sur un registre. La sémantique de chaque case dit toujours « nuit ». |
| `SlotChip.enEnregistrement` | prévu pour la grille du mois | non employé par « Mon mois » | L'enregistrement y est global par construction : la file part en bloc. Quarante contours qui pulsent sont un jeu de lumière, pas une information. La pulsation reste au catalogue pour un écran qui enregistrerait une case isolée. |
| Contour de la case sur un mois verrouillé | non traité | le filet prend l'encre du **verrouillage**, y compris pour « absent » | Un filet vermillon sur un mois verrouillé se lit comme une alarme alors que le verrouillage est un fait (§ Do's). La valeur reste portée par le glyphe et la texture. |
| Compteur `CountStat` sans plafond | « `null` signifie illimité, et c'est écrit en toutes lettres » | le mot « illimité » devient optionnel (`plafondAttendu`) | Un total de mois n'est pas un quota : « 12 illimité » ne veut rien dire, et le mot coûte la largeur de trois colonnes sur un téléphone. Le ticket 013 remplira le plafond et le mot disparaîtra. |
| Zone de bannière d'`AppScaffold` | enfant conditionnel | **toujours présente**, vide quand il n'y a rien à dire | Un enfant conditionnel décalait tous les suivants d'un cran à l'apparition d'une bannière : Flutter n'appariait plus les éléments, jetait le contenu et le reconstruisait. La grille y perdait sa position de défilement. |
| État pressé de `SlotChip` | non décrit | 8 % de l'encre d'état versés dans le fond, **rien d'autre** ne bouge | Rien ne doit se déplacer sous un doigt ganté, sous peine de rater la case voisine. Le `State` n'existe que sur une case actionnable. |
| Bannière `information` de date limite | listée parmi les variantes de bannière, et attendue sur la grille du mois | **non employée** sur « Mon mois » : la date limite vit dans la ligne d'état du sélecteur de mois (`lock_open` + « Ouvert jusqu'au 15 sept. ») | Une bannière permanente coûterait 48 dp de grille sur **tous** les mois ouverts pour redire ce que le bouton sélectionné dit déjà. La bannière `attention` (« Plus que 2 jours pour saisir octobre ») reste, elle, à J-3 et moins. |
| File d'écriture | non traitée | **gardée sur l'appareil**, une tranche par caserne, utilisateur et mois (`dispos.file.…` dans `localStorage`) | La bannière hors ligne promet que les modifications sont conservées. Sans persistance, un onglet fermé dans une remise sans réseau les emporte — et une disponibilité perdue est une garde non couverte. |
| Route `/mois` | annoncée dans § Navigation | onglet 0 de la coquille d'accueil + paramètre `?mois=AAAA-MM` | La coquille n'éclate pas en routes dans ce ticket ; l'URL reste néanmoins porteuse d'état. **Limite connue** : le paramètre ne survit pas à un démarrage à froid, l'écran de restauration de session réécrivant l'URL. |

### Coût de la case dense, mesuré

Le brief exige une case `dense` « sans état, sans animation, `const` autant que possible ». La
version livrée le tient : `SlotChip` est un `StatelessWidget`, le halo de focus est relu depuis le
`Focus` englobant au lieu d'un `setState`, et `Focus`, `MouseRegion`, `GestureDetector` et
`MetaData` n'existent que si la case est respectivement actionnable ou branchée sur un glissement.

Coût structurel d'**une** case dense, compté sur l'arbre d'éléments
(`test/core/widgets/matrice_dense_test.dart`) :

| | Éléments | Objets de rendu | `State` |
|---|---|---|---|
| Avant (ticket 004, première version) | 22 | 14 | 4 |
| **Après, case inerte** | **13** | **9** | **0** |
| **Après, case actionnable** | 25 | 13 | 3 |

Sur les 3 720 cases du pire cas (60 membres × 62 créneaux), une matrice en lecture seule économise
donc ~33 000 éléments, ~18 000 objets de rendu et **14 880 `State`**. La case actionnable compte
trois éléments de plus qu'avant parce que `Actions`, `Focus` et `MouseRegion` y sont désormais
explicites au lieu d'être empaquetés dans un `FocusableActionDetector` — mais elle crée un `State`
de moins, un objet de rendu de moins, et surtout **elle fonctionne**, ce qui n'était pas le cas.

Temps de première image, `flutter test` en mode debug sur un poste de développement :

| Cas | Cases construites | Fichier seul | Suite complète en parallèle |
|---|---|---|---|
| Sans virtualisation, cases inertes | 3 720 | ~830 ms | ~1 675 ms |
| Sans virtualisation, cases actionnables | 3 720 | ~940 ms | ~1 496 ms |
| **Virtualisée (ce que fera le ticket 016)** | 2 790 visibles | ~800 ms | ~1 769 ms |
| Défilement d'une page | — | ~600 ms | ~615 ms |

⚠️ **Ces durées ne sont pas un budget, et le tableau dit pourquoi.** La même mesure double selon
que le fichier tourne seul ou que la suite occupe tous les cœurs. Elles viennent d'un test de
widgets en mode debug, où les assertions et l'absence de JIT chaud dominent ; l'ancienne
implémentation mesure la même chose à ce bruit près. Le test ne pose donc qu'un garde-fou à 10 s,
qui attrape un blocage ou une régression d'un facteur dix, et rien de plus fin.

**Ce qui est acquis au ticket 004, c'est la structure**, elle parfaitement déterministe : une case
dense inerte ne coûte plus aucun `State`, et un tiers d'éléments en moins. **Ce qui reste à faire
au ticket 016**, c'est mesurer le budget de deux secondes sur un build `--profile` dans un vrai
navigateur, avec la matrice réelle et ses en-têtes collants.

## Écarts d'implémentation (ticket 024)

| Point | Ce que disait le document | Ce que fait le code | Pourquoi |
|---|---|---|---|
| `AppBanner` | « porte les faits qui changent tout ce qui est en dessous », sous la barre d'application, non fermable pour un état persistant | trois ajouts optionnels : `detail` (seconde ligne), `icone` (remplace celle de la famille), `onFermer` + `libelleFermer` | Une notification reçue au premier plan est un **événement**, pas un état : elle a un titre et un corps, une icône qui lui est propre (`notifications_active_outlined`) et elle se ferme. Une assertion interdit `onFermer` sur toute variante autre qu'`information`, la seule que le document déclare non persistante. |
| Position de la bannière | sous la barre d'application, dans l'écran | la bannière de push est posée **au-dessus du navigateur**, dans `MaterialApp.builder` | Un push ne choisit pas l'écran sur lequel il tombe. La poser dans chaque écran serait la répéter treize fois et l'oublier la quatorzième. Conséquence : pas d'`Overlay` au-dessus d'elle, donc **pas d'info-bulle** — le bouton de fermeture porte son nom par `Icon.semanticLabel`. |
| Durée d'un message passager | non traitée | **8 s**, pas les 3 à 5 s d'usage | Téléphone posé, regardé avec un temps de retard, souvent manipulé avec des gants. Rien ne se perd à l'effacement : le centre de notifications garde tout (ticket 026). |
| Réglage des notifications | l'écran « Profil » est le ticket 007 | le bloc réglé `ReglageNotifications` est posé dans l'onglet « Profil » de la coquille d'accueil | Le ticket 024 doit livrer le réglage que le PRD exige (§ 6.5) sans attendre l'écran complet. Le bloc déménagera tel quel. |
| `ContextePlateforme` | navigateur + mode d'affichage | porte en plus `libelleAppareil` (« Pixel 7 · Chrome ») | `push_tokens.device_label` se lit par un humain et se déduit du **même** agent utilisateur que la détection d'installation. Un second détecteur aurait divergé. Ni version, ni système : rien qui distingue deux téléphones du même modèle. |
| Route `/mois` et paramètre perdu au démarrage à froid | limite connue, inscrite aux écarts du ticket 011 | **corrigée** : `DestinationInitiale` garde la destination demandée pendant la restauration de session, et **seulement pendant celle-là** | La même panne cassait l'ouverture depuis une notification, cœur du ticket 024. Livrer l'un sans l'autre aurait donné une fonctionnalité qui ramène à l'accueil une fois sur deux. Ferme le ticket 039. Garder aussi à la déconnexion rejouerait l'écran d'un chef de centre pour le pompier suivant, sur un téléphone prêté. |
| Commutateur du réglage | non traité | `SwitchListTile`, **jamais `.adaptive`** | Plateforme enregistrée `web`, **une seule apparence partout** : la version adaptative rendrait un commutateur iOS sur Safari et un commutateur Material sur Android, pour le même réglage. |
| Écrans d'administration | non traité (manque antérieur) | `redirectionAuth` ferme `/admin` et tout ce qui est en dessous à qui n'administre pas la caserne | Les **données** étaient déjà protégées par RLS, mais l'interface s'ouvrait : un membre voyait des listes vides et un formulaire d'invitation qui échoue à l'envoi. Une porte fermée vaut mieux qu'une porte ouverte sur une pièce vide. Les écrans gardent leur propre phrase en seconde ligne. |

## Écarts d'implémentation (ticket 020)

| Point | Ce que disait le document | Ce que fait le code | Pourquoi |
|---|---|---|---|
| États d'attribution | quatre : proposé, accepté, refusé, annulé | **cinq** : « remplacé » s'ajoute entre « refusé » et « annulé », marque barrée et encre atténuée d'« annulé » (`#2A343A` sur `#E7EBEE`, `#C4CFD6` sur `#222C33` — contrastes déjà vérifiés), icône `Icons.swap_horiz`, libellé « Remplacé » | Le ticket 020 est le premier à **produire** `replaced` et `cancelled`, que le 019 lisait tous deux comme « annulé ». Les deux disent « cette attribution ne compte plus » et méritent la même marque ; ils ne disent pas la même chose — la garde a changé de main, ou elle n'existe plus. La distinction passe par l'icône et le libellé, **jamais par une cinquième couleur** : le registre n'a pas besoin d'une teinte de plus pour une nuance de sens. |
| Modale | « pas de modale pour une tâche qui ne demande ni interruption ni protection » | une confirmation nommée avant toute réattribution et toute annulation | Celle-ci demande une protection : le geste **sort de l'application** — un téléphone sonne — et ne se défait pas. C'est la différence exacte avec l'attribution en brouillon, qui n'en ouvre aucune parce qu'elle se défait d'un clic. La feuille porte **tous** les avertissements à la fois (hors disponibilité, titulaire sortant) : une confirmation par sujet apprendrait à cliquer « Oui » sans lire. |
| Ligne du suivi | « les lignes ne sont pas cliquables » (écart du ticket 019) | un **bouton nommé** en fin de ligne, sur les seuls créneaux qui portent un refus, une annulation ou un trou | La règle du 019 visait le faux affordance ; elle est tenue autrement. La ligne entière reste inerte — elle porte trois cibles de lecture et s'ouvrirait par accident au doigt, avec des gants, pendant un défilement —, et le bouton n'apparaît jamais sur un créneau qui n'a rien à réparer. |
| Bandeau d'information dans un panneau | non traité | `secondary-container`, icône `Icons.campaign`, **sans filet coloré à gauche** | Un liseré de 4 dp sur un bloc d'information est une habitude, pas une décision (`§ Don't`). Le fond et l'icône portent déjà l'état. |

## Écarts d'implémentation (ticket 027)

| Point | Ce que disait le document | Ce que fait le code | Pourquoi |
|---|---|---|---|
| Destination 3 | `groups` / `groups_outlined`, « Planning », route `/planning` | `event_available_outlined` / `event_available`, **« Astreintes »**, route `astreintes` — elle porte « Mes astreintes » | Le brief du ticket 004 notait que la place de cet écran serait à revoir à l'usage. Le compte reste à cinq, l'ordre ne bouge pas, et l'icône cesse de promettre « les autres » sur une destination qui, tant qu'un planning reste `published`, ne montre que soi. « Quand suis-je d'astreinte » (027) et « qui est d'astreinte » (023) lisent la **même** table sous la **même** politique : ce sont deux filtres d'une vue, pas deux écrans, et le ticket 023 s'y rangera derrière un sélecteur à deux segments. D'où « Astreintes », qui couvre les deux (`design/027 § 4`). |
| Bannière `hors-ligne` | « Hors ligne. Tes modifications partiront au retour du réseau. » | sur un écran de consultation : « Hors ligne. » + seconde ligne « Dernière mise à jour : il y a 2 h. » Même variante, même ocre, même icône | Cet écran **n'écrit rien**. Promettre l'envoi de modifications inexistantes serait faux ; ce qu'il faut dire, c'est l'âge de ce qu'on lit. Une seconde phrase, `attention`, couvre le cas « en ligne mais lecture ratée » : rien n'est cassé, c'est juste vieux. |
| Grille calendaire en compact | écart du ticket 011 : « le calendrier à sept colonnes reprend dès `expanded` » | le calendrier existe **aussi en compact**, derrière une bascule explicite, avec une marge de grille ramenée à 8 et un écart de 4 | L'écart du 011 vise une grille dont **les 62 cases** se touchent : `7 × 48 + 6 × 8 = 384 dp` contre 328. Ici trois ou quatre cases par mois sont actionnables et les autres sont du texte ; à marge 8 et écart 4, une case fait 45,7 dp sur un téléphone de 360. La liste reste la vue par défaut, et au-delà de ×1,6 d'échelle de texte le calendrier cède la place à la liste, le bouton désactivé disant pourquoi. |
| Contrôle à deux choix | non traité ; `§ Shapes` proscrit la pastille sur un contrôle | la bascule « Liste » / « Calendrier » est faite de **deux blocs à rayon `controle`**, le choisi en `primary-container` avec `Icons.check` en tête — jamais un `SegmentedButton` | Le segmenté Material est en gélule (`StadiumBorder`). La composition du sélecteur de mois du ticket 011 existe déjà et tient les trois signaux : la coche, le fond, `Semantics(selected:)`. |
| Marque d'un jour d'astreinte | au ticket 004, `§ Attribution` : « accepté » = `#05321A` sur `#CFE9D8` | la case du calendrier prend l'encre de **`etat-disponible-plein`** (`#7655FA`, glyphe blanc) et porte l'**icône du créneau** | Le vert pâle d'« accepté » ne tient pas en niveaux de gris (0,78 contre 1,00 pour une case vide), et c'est la lecture signature de l'écran. L'encre est prise pour sa **valeur**, pas pour son sens : sur cet écran « accepté » est universel et ne distingue rien — c'est le créneau qui porte l'information, par son glyphe et par la phrase annoncée. La règle « jamais la couleur seule » reste tenue par quatre signaux. |
| Raison d'un contrôle désactivé | « une raison affichée à côté du contrôle » | pour les deux flèches de mois du calendrier, la raison est le **nom accessible** de la flèche et son info-bulle | Une phrase permanente sous la barre de mois coûterait 20 dp de grille pour redire ce qu'une flèche grise dit déjà. Le nom porte la raison : un lecteur d'écran annonce « Tu n'as pas d'astreinte avant ce mois, bouton, désactivé ». La règle tient pour les boutons de `PrimaryButton`, qui gardent leur phrase. |
| Restauration de session | non traitée | **la caserne du membre est gardée sur l'appareil** (`core/session/appartenances_locales.dart`), et `appartenancesProvider` y retombe **quand, et seulement quand, `memberships` échoue sur le transport**. Ce qui revient du stockage est **toujours un simple membre** (`Appartenance.commeMembre`, appliqué dans le provider) | Sans ce repli, un démarrage à froid sans réseau s'arrête sur « Pas de connexion » avant d'atteindre le moindre écran : la session se restaure toute seule, la caserne non. Aucun cache de données ne servait donc dans la seule scène qui les justifie. Le repli est borné au réseau : masquer un refus ferait croire à un membre retiré de sa caserne qu'il y appartient encore. Et un rôle qu'on n'a pas pu revérifier n'accorde aucun privilège — même règle que `RoleMembre.depuisSql` ; conséquence assumée : hors ligne, un chef de centre perd l'onglet « Admin », qui ne lui ouvrirait que des listes vides (écart du ticket 024). |
| Déconnexion | « garder [la destination en attente] à la déconnexion rejouerait l'écran d'un chef de centre pour le pompier suivant » (écart du ticket 024), dit de la seule destination | la règle vaut pour **tout ce que l'appareil garde** : `DeconnexionController` efface les deux caches — la caserne et l'instantané des astreintes — **avant** de fermer la session | Le second document porte les **noms des autres membres du créneau** : des données de tiers. Avant la fermeture, parce qu'après, ni l'identifiant du membre ni celui de la caserne ne sont plus lisibles, et une clé qu'on ne sait plus composer ne s'efface pas. Les pannes de stockage restent avalées : un effacement qui échoue ne retient personne dans une session qu'il veut quitter. Tout nouveau cache local devra être branché ici. |

## Écarts d'implémentation (ticket 023)

| Point | Ce que disait le document | Ce que fait le code | Pourquoi |
|---|---|---|---|
| Deuxième contrôle à deux choix | `§ Écarts, ticket 027` : la bascule « Liste » / « Calendrier » est faite de deux blocs à rayon `controle`, jamais un `SegmentedButton` | la composition est **extraite** dans `BasculeDeux`, et les deux bascules de l'écran — « Moi » / « La caserne » et « Liste » / « Calendrier » — en sont deux instances | Le ticket 023 en demandait une seconde. Deux copies auraient divergé au premier réglage de contraste, et c'est exactement ce que le système existe pour empêcher. Les trois signaux de l'état choisi — coche, fond, `Semantics(selected:)` — vivent désormais à un seul endroit. |
| Deux bascules empilées | non traité | la bascule de vue **appartient à la portée « Moi »** et vit dans son sous-arbre ; « La caserne » n'a qu'une forme, donc pas de seconde rangée | Deux rangées de 48 dp coûteraient 128 dp de chrome sur un téléphone de 844, pour deux questions qui ne sont pas du même ordre. Le chrome reste comparable des deux côtés : portée + vue à gauche, portée + mois à droite. |
| Un fait qui décrit le contenu, pas l'écran | `§ Signature Component` : une seule `AppBanner` à la fois, par ordre de priorité | « Planning publié, pas encore validé » est un **bloc réglé dans le contenu**, premier élément de la liste, pas une bannière | La bannière ne porte qu'un fait à la fois : un pompier hors ligne aurait perdu exactement l'explication dont il a besoin. Et ce fait ne décrit pas l'écran, il décrit **le mois affiché** — il se déplace donc avec lui. Ocre d'attente et `Icons.hourglass_top` de l'état **proposé** (`§ Attribution`), sans filet coloré à gauche (`§ Écarts, ticket 020`). |
| Liste de personnes | non traité ; `§ Chips` décrit la case du registre, pas une personne | les noms d'un créneau sont **du texte qui s'enroule**, séparés par un point médian, jamais des pastilles | Douze pastilles de 28 dp mangeraient trois hauteurs d'écran pour une information qui se lit d'un coup. Le lecteur lui-même est en tête, marqué par `Icons.person` plein, l'encre `accent-texte` et le mot « Toi » — trois signaux, et la phrase annoncée dit « avec toi et … ». |
| Barre de mois | `§ Écarts, ticket 027` : la raison d'une flèche désactivée est son nom accessible et son info-bulle | la barre est **extraite** dans `BarreMois`, partagée par le calendrier de « Mes astreintes » et le registre de la caserne, chacun fournissant ses libellés | Même composant, mêmes cibles de 48 dp, deux jeux de raisons : « Tu n'as pas d'astreinte avant ce mois » d'un côté, « Aucun planning publié avant ce mois » de l'autre. Le sélecteur de la caserne marche **de planning en planning** et non de mois en mois : un mois sans planning n'est pas une destination. |
| Un état vide qui n'en est pas un | `§ Don't` : pas d'état vide muet | **un échec de lecture ne devient jamais un état vide** : la condition porte sur le contenu (`mois.isEmpty`) et non sur `hasValue`, et une liste vide encore en chargement ne décide d'aucune redirection dans `etatAuthProvider` | `AsyncValue` garde la valeur du calcul précédent à côté d'une erreur ou d'un chargement. Sans ces deux gardes, une caserne parfaitement organisée s'affichait « Aucun planning publié », et un membre parfaitement rattaché « Aucune caserne » — sur un écran qui ne propose que la déconnexion. Vu dans Chrome, API arrêtée (`design/023 § 10`). |
| Caches locaux | `§ Écarts, ticket 027` : « tout nouveau cache local devra être branché ici » | `CachePlanningCaserne` est effacé par `DeconnexionController`, **avant** la fermeture de session, et son effacement balaie **tout le préfixe** du couple caserne/membre | Il range **une entrée par mois visité** : effacer une clé en laisserait cinq. C'est le cache le plus chargé en données de tiers du produit — il porte les noms de toute la caserne. Il ne décide d'aucun droit : un mois gardé alors que le planning n'était que publié ne contient que les créneaux du lecteur, il ne peut donc pas révéler ce que la base refusait. |


## Écarts d'implémentation (ticket 029)

| Point | Ce que disait le document | Ce que fait le code | Pourquoi |
|---|---|---|---|
| Une sixième famille d'états | `§ Named Rules` en nomme cinq : disponibilité, créneau, attribution, planning, période, hors ligne | les cinq statuts d'abonnement **réemploient** les descripteurs existants — ocre de `attribution.propose` pour l'essai, vert de `planning.valide` pour l'actif, gris hachuré de `periode.verrouillee` pour le suspendu, gris atténué de `planning.archive` pour le résilié — via `StatusBadge.descripteur`, et **aucune encre nouvelle n'est posée** | Une sixième famille dans `AppStatusColors` aurait ajouté dix couleurs à vérifier au contraste pour un écran qu'un chef de centre ouvre trois fois par an. Le seul descripteur composé sur place est « paiement en retard », et il est bâti sur `error` / `errorContainer` du thème. |
| Suspendu n'est pas rouge | `§ Do's` : « Traiter *verrouillé*, *suspendu*, *annulé* comme des faits gris » | « Caserne suspendue » et « Abonnement résilié » sont **gris**, hachuré pour le premier ; seul « Paiement en retard » est rouge | Une caserne suspendue n'est pas en panne, elle est en lecture seule, et c'est réversible. Le retard de paiement, lui, porte une action à faire et une échéance réelle — quatorze jours — donc le rouge y dit quelque chose. |
| Un contrôle inerte plutôt que caché | `§ Buttons` : un bouton désactivé affiche sa raison à côté | sans compte chez le prestataire, les deux boutons « S'abonner » **restent à l'écran**, grisés, avec « Bientôt disponible. » — et les tarifs restent affichés | Cacher les formules laisserait un chef de centre en essai sans réponse à « ça coûtera combien ? », qui est la question qu'il se pose. C'est le seul endroit du produit où l'on montre délibérément un contrôle inerte plutôt que de le retirer. |
| `EnteteSection` sans compte | `§ Components` : l'en-tête porte un titre **et** son compte | `compte` devient facultatif ; une section qui n'ouvre pas une liste n'affiche plus de ligne vide sous son titre | Les trois sections de cet écran ne comptent rien. Une chaîne vide aurait laissé un interligne de 20 dp qu'aucune règle ne justifie. |
| Sortie de l'application annoncée | non traité ; `§ Écarts, ticket 024` : « la fenêtre du navigateur est brutale, on l'annonce une ligne avant » | « Gérer mon abonnement » porte `Icons.open_in_new` **et** la phrase « Carte bancaire, factures et résiliation se règlent sur la page sécurisée de Stripe. » | Même règle qu'au 024, appliquée à un onglet plutôt qu'à une permission. Et quand le navigateur bloque l'onglet, l'écran le dit et nomme la sortie : un bouton qui ne fait rien en silence est pire qu'une erreur. |
| Nombres en Mono | `§ Typography` : tout nombre qui s'aligne prend le cut Mono et `tabularFigures` | les deux montants prennent `AppTextStyles.nombre` | Deux lignes de tarif qui ne s'alignent pas se comparent mal, et comparer est précisément ce qu'on fait sur cet écran. |

## Écarts d'implémentation (ticket 007)

| Point | Ce que disait le document | Ce que fait le code | Pourquoi |
|---|---|---|---|
| Destination « Profil » | `§ Navigation` : route `/profil` | l'écran reste **l'onglet 3 de la coquille d'accueil**, comme « Mon mois » et « Astreintes » | Même écart que les tickets 011 et 027 : la coquille n'a pas encore éclaté en routes. L'onglet voyage dans `?onglet=`, le retour du navigateur marche, et le jour où les routes arrivent, `ProfilScreen` est déjà un écran complet qui construit son propre `AppScaffold`. |
| Blocs réglés | `§ Cards / Containers` : la décoration est décrite, pas fournie | elle est **extraite** dans `BlocRegle` (`features/profil/presentation/widgets`), avec `LigneLecture` pour une valeur non modifiable et sa raison | Quatre blocs sur un même écran, plus celui du ticket 024 qui portait déjà la même décoration en dur : cinq copies auraient divergé au premier réglage de contraste. `LigneLecture` porte aussi la règle « libellé invisible quand le titre du bloc le dit déjà » — « Ta caserne » deux fois de suite se lit deux fois, à l'œil comme à l'oreille. |
| Contrôle inerte plutôt que caché | `§ Do's` : « Expliquer pourquoi un contrôle est désactivé, à côté du contrôle » | la **langue** et l'**adresse de connexion** sont des lignes en lecture, chacune avec sa raison — jamais un sélecteur à une entrée ni un champ grisé | Un menu de langues à un seul choix promet une traduction qui n'existe pas ; un champ d'adresse grisé promet qu'on pourra un jour le remplir ici, alors que c'est l'identifiant du compte. Dire le fait et la raison coûte deux lignes et ne promet rien. |
| Groupe de choix | non traité | le sélecteur de caserne est un `RadioGroup` de `RadioListTile`, **jamais un menu déroulant**, et il n'apparaît qu'à partir de **deux** appartenances actives | Deux ou trois entrées tiennent à l'écran ; un menu cache jusqu'à l'existence du choix, sur un écran qu'on ouvre deux fois par an. La caserne choisie porte `Semantics(selected:)` en plus du bouton radio : l'état n'est jamais porté par la seule teinte. |
| Modale pour un acte définitif | `§ Don't` : « pas de modale pour une tâche qui ne demande ni interruption ni protection » | la suppression de compte passe par une **feuille de bas d'écran** qui porte quatre faits : ce qui part, ce qui reste, que c'est définitif, et comment sortir | Celle-ci demande interruption **et** protection, mais elle demande surtout de la place pour lire. Un `AlertDialog` de téléphone ne montre pas les quatre. Pas de phrase à recopier pour confirmer : le public a une aisance numérique variable, et deux touches réfléchies valent mieux qu'une formule recopiée sans lire. |
| Deux boutons, une seule raison | `§ Buttons` : un bouton désactivé affiche sa raison à côté | « Annuler », désactivé pendant l'appel, porte sa raison en **annonce seulement** (`raisonVisible: false`) ; la ligne d'état commune aux deux boutons la porte à l'écran | Écrire « Suppression en cours… » sous « Annuler » **et** sous la paire est du bruit, pas une explication. L'invariant tient : la raison reste obligatoire et reste annoncée. |
| Caches locaux | `§ Écarts, ticket 023` : « tout nouveau cache local devra être branché ici » | l'effacement quitte `DeconnexionController` pour `OubliLocal` (`core/session/oubli_local.dart`), partagé par la déconnexion **et** la suppression de compte. Il boucle sur **toutes** les appartenances, pas seulement la caserne affichée, et emporte la file de saisie hors ligne | Le sélecteur de caserne rend le cas réel : une personne à deux casernes laissait derrière elle l'instantané de l'autre, qui porte les noms de ses collègues. Et une suppression doit laisser l'appareil **au moins** aussi propre qu'une déconnexion — les faire diverger, c'est garantir qu'une des deux prendra du retard. |
| Route impérative et routeur | non traité | la feuille de suppression se **referme avant** que la session tombe : le contrôleur fait le serveur et le ménage, la feuille se ferme, puis l'appelant ferme la session | `showModalBottomSheet` pousse une route que `go_router` ne connaît pas. Fermer la session pendant qu'elle est ouverte remplace toutes les pages du routeur et laisse la feuille seule au sommet de la pile : **écran blanc jusqu'au rechargement**. Vu dans Chrome, PWA, après une vraie suppression (`design/007-profil.md § 8`). La règle vaut pour toute feuille dont l'action change l'état d'authentification. |

## Écarts d'implémentation (ticket 034)

| Point | Ce que disait le document | Ce que fait le code | Pourquoi |
|---|---|---|---|
| Remise d'un fichier | non traité | l'export passe par un `Blob`, une ancre `download` et un clic programmatique (`core/plateforme/telechargement_web.dart`), **jamais `window.open`** ; sur iOS installé, quand `navigator.canShare({files})` répond oui, il passe par la feuille de partage du système | `§ Zones sûres et chrome PWA` pose que le produit tourne en `display-mode: standalone`. Dans ce mode il n'y a pas de barre d'adresse : `window.open` expédie la personne hors de l'application, quand le navigateur ne refuse pas tout simplement. Et l'attribut `download` reste inégal sur iOS — « Enregistrer dans Fichiers » y est le geste attendu. Trois résultats distincts remontent à l'écran (`enregistre`, `partage`, `annule`) parce qu'ils se disent différemment. |
| Confirmation d'une action | `§ Do's` : « Nommer l'action dans le bouton et la sortie dans l'erreur » | la ligne d'état sous le bouton d'export **nomme le fichier** (« Fichier enregistré : astreinte-sp-export-2026-09-21.json »), en `liveRegion` | Dans une PWA installée, il n'y a **pas de barre de téléchargement** : si l'écran ne dit pas que le fichier est parti, rien ne le dit. Un export RGPD dont on ne sait pas s'il a abouti ne vaut rien. Symétriquement, un partage refermé n'affiche **rien** : ce n'est pas une panne, et une fausse erreur apprend à ne plus lire les vraies. |
| Pages de texte long | non traité | la politique de confidentialité et les mentions légales sont **des routes** (`/legal/confidentialite`, `/legal/mentions`), joignables dans les quatre états d'authentification, et leur contenu est une **donnée** (`features/legal/domain/documents_legaux.dart`) que le widget se contente de rendre | Une politique de confidentialité doit se lire **sans compte** — par une mairie, par un candidat à l'invitation — et avoir une URL qu'on colle dans une délibération. Quatre pages de texte dans un `Column` ne se relisent pas et contrediraient « chaque chaîne dans `AppStrings` » ; en données, la liste des sections se lit comme un sommaire et un diff nomme le paragraphe qui change. |
| Ce qui n'est pas décidé | non traité | chaque mention qui relève du propriétaire — raison sociale, adresse, hébergeur contractuel, durées légales — s'affiche dans un bloc réglé `surface-container-high`, icône `edit_note` + libellé « À compléter avant mise en service » | Le produit est destiné à des casernes françaises. Une mention légale inventée se croit ; une mention trouée se corrige. Le bloc est porté par icône + libellé, la couleur en quatrième (`§ Do's`), et il ne se ferme pas. |
| Deux liens en pied de bloc | `§ Cibles tactiles` : 48 dp, 8 dp d'écart | « Confidentialité » et « Mentions légales » sont deux `TextButton` de 48 dp dans un `Wrap`, jamais deux mots collés dans un `RichText` | Un lien de la taille d'un mot n'est pas une cible avec des gants. Le `Wrap` les fait passer l'un sous l'autre à grande échelle de texte, au lieu de déborder. |

## Écarts d'implémentation (ticket 061)

Le monde visuel du ticket 004, bleu nuit sur blanc et Atkinson seule, est remplacé au ticket 061 par le monde décrit ci-dessus : accent indigo, vert pour l'accepté, orange pour l'attente, rose pour le refus, violet décoratif, Archivo sur les titres. Tous les écrans ont changé d'apparence par les tokens, sans changement de structure. Neuf endroits lisaient une teinte qui a changé de métier et ont été rebranchés : le texte d'attente sur son propre bloc (4,22:1, sous le seuil) passe sur le texte sur fond ; le texte en `primary` sur le fond de grille (3,96:1) passe sur `accent-texte`. Les états « accepté », « validé » et « enregistré » avaient suivi « disponible » vers l'indigo par cascade de tokens ; ils sont revenus au vert, avec des tokens nommés `etat-accepte-*`. Les boutons segmentés et les puces sélectionnaient en vert, le défaut de Material ; ils sélectionnent en indigo. Les mesures sont dans `test/core/theme/contraste_test.dart` et `test/core/theme/selection_et_accepte_test.dart`.
Un écart au brief, assumé : le brief écrit « orange et rose, jamais en texte ». Le code introduit deux versions assombries, `tertiary #9F6224` (4,94:1) et `error #BB285D` (5,87:1), pour les libellés d'état qui doivent être lus ; les teintes vives de la charte, `orange-vif` et `rose-vif`, restent réservées aux remplissages.


## Écarts d'implémentation (ticket 061b)

La coquille de l'admin sur grand écran : navigation latérale, en-tête, bandeau du mois, bande de semaine.

| Point | Ce que disait le brief | Ce que fait le code | Pourquoi |
|---|---|---|---|
| La colonne de navigation | « le rail étendu devient une colonne » | `ColonneNavigation`, bâtie sur `NavigationDrawer`, **à partir d'`expanded`** et non de `large` seulement ; largeur inchangée, 180 | Le rail étendu vivait déjà dès 840 : n'en changer qu'à 1200 aurait fait deux navigations à maintenir pour une même classe de composition. |
| La barre d'application sur grand écran | non traité | **elle disparaît** dès `expanded` : le titre et les actions passent dans `EnTeteTravail`, en tête de la zone de travail | La navigation n'est plus sous l'écran mais à côté de lui ; une barre pleine largeur au-dessus des deux aurait fait un troisième bandeau pour redire le nom de l'endroit. Aucun écran ne perd de sortie : ceux qui portent `AppScaffold` ont leur navigation, et ceux qui n'en ont pas gardent `BoutonRetour`. |
| Le nom de la caserne en titre | « nom de la caserne à gauche en `titleLarge` » | `AppScaffold.caserne`, **facultatif** : seul l'écran du planning admin le passe pour l'instant ; les autres gardent leur titre d'écran | Sur l'écran de l'admin, le mois titre le contenu (sélecteur de la barre de commande) et la destination dit l'endroit. Ailleurs, remplacer « Membres » par le nom de la caserne aurait retiré le seul repère de l'écran sans rien mettre à la place. Les écrans suivants le passeront au fur et à mesure. |
| La cloche et le compte | « à droite la cloche puis un bouton de compte » | `AppScaffold.actionsEnTete`, **ignorées hors grand écran** | En `compact`, la cloche vit déjà dans `actions` des cinq écrans de la coquille, et la barre d'application de l'admin n'a pas la place de deux actions de plus. Le compact ne change pas. |
| L'unité du bandeau | « créneaux couverts », « à pourvoir (requis moins pourvus) », « réponses en attente » | l'unité est le **créneau** partout, et « À pourvoir » compte les créneaux qui manquent de monde, refus compris | La barre de répartition doit partager l'unité des chiffres qu'elle surplombe, sinon la légende et le nombre se contredisent. Avec l'effectif requis de 1 — le réglage par défaut — les deux comptes coïncident. |
| Les libellés de la légende | quatre parts : couvert, à pourvoir, refusé, non saisi | « Couverts », **« À remplir »**, « À réattribuer », « Sans astreinte requise » | « À pourvoir » est déjà le libellé d'un des trois chiffres, et il compte autre chose (les refus avec). Deux fois le même mot dans le même bloc pour deux comptes différents est un piège. |
| Le rose de la barre | « rose refusé à réattribuer » | la part existe, mais elle vaut **zéro** tant que le dépôt du planning (ticket 017) ne rend que les attributions actives | Les refus sont filtrés à la lecture (`_statutsActifs`) ; les compter aurait demandé une lecture de plus, que le chantier interdit. La part se remplira sans changer une ligne d'écran le jour où le dépôt les rendra. |
| Le mois en titre du bandeau | « le mois en titre `titleLarge` au-dessus des chiffres » | **le bandeau ne l'écrit pas** : la ligne des trois chiffres part du bord gauche du bloc, sur la verticale de la barre de répartition, en `large` comme en `compact` | Le mois n'est écrit qu'une fois, dans le **sélecteur** en tête de la barre de commande : c'est lui l'élément interactif, et lui seul porte l'état de la période (« Ouvert jusqu'au 15 août »). Le répéter trente points plus bas en `titleLarge` posait une question au lecteur — deux mois, deux endroits, lequel commande ? La place libérée ne revient pas à un intitulé neutre : mesuré à l'écran, un « Couverture du mois » en `labelSmall` aligné sur les libellés des chiffres se lit comme un quatrième compteur privé de son nombre. Elle revient aux chiffres. |
| Fenêtre courte | non traité | **seuils mesurés, pas estimés** : sous 334 points sous la barre de commande, le bandeau se réduit à sa ligne de chiffres ; sous 258, il s'efface avec la bande. Le bloc complet est resserré à `md` et mesuré à 134 points, la bande à 72, la réserve de grille à 128 (quatre lignes) | **La barre de répartition est un livrable, elle doit se voir sur un portable.** Le premier seuil, 440, avait été posé au jugé : mesuré, un 13 pouces à 1280 × 720 avec sa barre d'adresse ne laisse que **341 points** sous la barre de commande, et la barre du mois ne s'affichait donc jamais en dessous de 900 de hauteur. Resserré et recalculé, le bloc passe à 720. Ce qu'il en coûte est dit sans le farder : à 720 la grille garde 135 points, c'est-à-dire son bloc épinglé (84, ou 112 dès que le planning existe) et une ligne et demie de membres ; les quatre lignes entières ne se lisent qu'à partir de 420 points de place, soit une fenêtre de 800. Et dès que le planning existe, le fil « Publier » prend 70 points de plus : à 1280 × 720 il ne reste que 271 points et le bandeau retombe à sa ligne de chiffres. **La matrice garde la main pour autant** : un résumé qui laisserait deux lignes de grille aurait remplacé l'écran qu'il surplombe, et un filtre qui ne rend rien efface aussi le résumé — l'état vide et sa sortie prennent toute la place. |
| Les trois compteurs | trois chiffres avec leur libellé | **le nombre au-dessus du libellé**, aux deux largeurs | « Réponses en attente » se replie sur deux lignes là où « À pourvoir » tient sur une : le nombre placé dessous descendait d'une ligne pendant que ses deux voisins restaient en haut, et les trois chiffres ne se lisaient plus d'un seul balayage. En tête, ils partagent la même ligne quel que soit le repli. La ligne d'attaque du bloc n'a pas bougé — le premier nombre est sur la verticale de la barre de répartition, là où était le premier libellé. |
| Le jour visé dans la bande | non traité | la pastille cliquée porte un **liseré `outline` de 2 points** en plus de son remplissage, et `selected` dans la sémantique ; elle s'efface dès que la grille défile hors de ce jour | Après un clic, la matrice amenait la colonne au bord gauche et la bande restait identique : on perdait de vue ce qu'on venait de demander. La marque dit « la matrice est posée là », pas « tu as cliqué ici » — d'où l'effacement au défilement, branché sur les notifications de la grille. Jamais la couleur seule : liseré **et** `selected`. Elle coexiste avec la pastille d'aujourd'hui, les deux peuvent tomber sur le même jour. |
| La bande de semaine en `compact` | non demandée | absente | La vue par jour du 016 a déjà son ruban de jours, et il n'y a pas de matrice à faire défiler sur un téléphone. Le bandeau, lui, y reste, réduit à sa ligne de trois chiffres. |

Tranché au chantier **061c**, section suivante : la bande de semaine disparaît, sa pastille passe dans l'en-tête de dates de la matrice, et la hauteur est reprise là où elle était vraiment prise — la barre de commande et le fil « Publier ».


## Écarts d'implémentation (ticket 061c)

Les dates de la matrice et la barre de commande de l'admin, sur grand écran.

| Point | Ce que disait le brief | Ce que fait le code | Pourquoi |
|---|---|---|---|
| La bande de semaine | `design/061 § 5` : « les jours du mois en ligne, le jour courant en pastille indigo, défilement horizontal, sert à faire défiler la matrice à la semaine visée » | **elle est retirée**, avec `DefilementJour`, le paramètre `defilementJour` de `GrilleMatrice`, la marque du jour visé et leurs tests. Sa forme — abréviation du jour au-dessus du numéro, pastille `primary-container` / `on-primary-container` pour aujourd'hui, graisse et `surface-dim` pour le weekend et les fériés, étoile du férié — passe dans `EnteteJour`, l'en-tête épinglé de la grille | La référence du propriétaire n'a **qu'une rangée de dates**, et c'est l'en-tête de sa grille. Livrée au 061b, la bande répétait cet en-tête à un pas différent — 56 points contre 62 — si bien que les deux rangées de numéros ne tombaient jamais en face l'une de l'autre : deux calendriers du même mois, décalés, l'un sous l'autre. Et sa seule valeur propre, amener une colonne au bord gauche, **déplaçait le contenu sous le pointeur** : on cliquait le 12, la grille sautait, et la case qu'on visait n'était plus là où la main l'attendait. Ce qui reste est une rangée de dates qui **nomme** les colonnes, sans les commander : ni `InkWell`, ni `button` dans la sémantique. |
| La hauteur de l'en-tête des dates | non traité | **60 points**, mesurés avec les vraies polices du produit (`test/support/polices.dart`) : la pastille 36, l'écart 2, les deux icônes de créneau 14. Plafond du chantier : 64 | Quatre points de plus qu'au ticket 016, où l'en-tête portait une initiale nue : c'est le prix de l'abréviation au-dessus du numéro, et il est payé par les 72 points que la bande rend. Le bloc épinglé passe de 84 à 88 points sans planning, de 112 à 116 avec la ligne des créneaux. La parité au pixel près tient parce que `CoinFige` et `EnteteJour` lisent la **même** constante `GeoMatrice.hauteurEntete` : si elles divergeaient, le nom ne serait plus en face de ses cases. |
| Le seuil du bandeau | `§ Écarts, ticket 061b` : 334 points sous la barre de commande pour le bloc complet, 258 pour sa ligne de chiffres | **262 et 186** : les 72 points de la bande sortent du compte | Le seuil ne paie plus que ce qui s'affiche. Conséquence mesurée à 1280 × 720, avec un planning créé : le bandeau **complet**, barre de répartition comprise, s'affiche — au 061b il retombait à sa ligne de trois chiffres dès que le planning existait. |
| La barre de commande | non traité au 061 ; quatre étages hérités des tickets 016 et 017 | **deux rangées de contrôles de 48 points** dès `expanded`. Rangée 1 : le mois, la recherche, les trois puces. Rangée 2 : « Saisir à la place d'un membre », l'action du planning et sa raison, la légende poussée au bord droit avec l'indicateur d'enregistrement | C'est là que la hauteur de cet écran était prise : 266 points sans planning, 230 plus 70 de fil « Publier » avec. Le bandeau du 061b, lui, n'en coûtait que 134 et s'effaçait le premier. Mesuré à 1280 avec les vraies polices : **266 → 124** sans planning. |
| « Publier » | `§ Écarts, ticket 016` : le fil d'actions du bas | **dans la seconde rangée de la barre dès `expanded`**, dans le fil du bas en `compact` | Le fil ne défile pas avec la matrice — c'était sa raison d'être — mais il coûte 70 points **sous** elle, et la barre de commande ne défile pas non plus sur grand écran. En `compact`, la barre vit dans le défilement de la vue par jour : un bouton « Publier » qui s'en irait au défilement serait un bouton qu'on cherche, donc il y reste en bas. |
| Le sélecteur de mois | `§ Écarts, ticket 011` : le mois et sa ligne d'état, 64 points | **48 points sur une ligne** dans la barre de l'admin : « Octobre 2026 · jusqu'au 15 sept. », icône de l'état devant. Deux lignes et 64 points partout ailleurs, `compact` compris | Le mot « Ouvert » est redondant avec l'icône `lock_open` posée juste devant, et il coûtait quarante points dans une rangée qui n'en avait pas un de trop. Sur téléphone, le bouton garde ses deux lignes : « Ouvert jusqu'au 15 sept. » à la suite du mois s'y replierait, et un bouton replié sur deux lignes n'est pas plus court qu'un bouton à deux lignes. |
| Le champ de recherche | `§ Inputs / Fields` : « libellé toujours visible **au-dessus** du champ » | dans la barre de grand écran, le libellé est **dedans**, flottant : il remonte à la saisie et ne disparaît jamais. `ChampTexte` et son libellé empilé restent la forme de tous les formulaires, et celle de la barre sur téléphone | `ChampTexte` fait 78 points : dans une rangée de 48, il n'y a pas de place au-dessus. Ce que la règle protège — qu'on sache ce qu'on tape après avoir commencé à taper — tient toujours : un libellé flottant remonte, il ne s'efface pas comme un texte d'invite. L'écart est borné à cette barre. |
| Les explications des boutons | `§ Do's` : « un contrôle désactivé porte sa raison à côté de lui » | les explications de « Créer le planning » et de « Publier » sont **réécrites sur une ligne** et posées à côté du bouton, sans largeur maximale et sans `ellipsis` | Trois lignes de texte dans une rangée de 48 points, c'est une rangée de 96. Ce qui devait rester est resté : le nombre de créneaux et l'effectif requis pour la création, le nombre de pompiers prévenus pour la publication. Ce qui est parti est la phrase qui les enrobait. Rien n'est tronqué : c'est la `Wrap` qui passe à la ligne, jamais le texte qui se coupe. |
| Le nombre de mois ouverts | non traité | la première rangée est une `Row` : le sélecteur y reçoit **une part** de la largeur et défile à l'intérieur, au lieu de pousser les puces à la ligne | `periodesProvider` rend **toutes** les périodes de la caserne, toutes années confondues : le sélecteur est un ruban qui s'allonge d'un mois par mois. Dans une `Wrap`, il prenait sa largeur intrinsèque et la rangée suivait ; dès deux mois ouverts, les puces passaient à la ligne et la barre faisait 48 points de plus. Il défilait déjà horizontalement depuis le ticket 011 : on lui rend ce rôle au lieu de laisser la rangée le subir. |
| Ce que le plafond de 130 points ne tient pas | non traité | quand le planning existe **et** qu'il reste des créneaux à pourvoir, la seconde rangée prend deux lignes et la barre monte à **184 points**. C'est mesuré et testé, pas subi | Cette rangée porte alors trois éléments que le chantier n'avait pas budgétés, venus des tickets 017 et 018 : l'état du planning, le témoin « Direct » du canal temps réel et « Proposer automatiquement ». À eux trois, 418 points dans une rangée qui en a 95 de libres une fois « Saisir à la place d'un membre » (252) et la légende (280) servis. Aucune réécriture de texte ne comble 338 points ; seule une **réaffectation** le ferait — l'état du planning et le témoin direct descendraient dans le bandeau du mois, qu'ils décrivent —, et elle dépasse ce chantier. Ce qui est tenu : 184 reste très en dessous des 300 points que la barre et son fil coûtaient au 061b. |
