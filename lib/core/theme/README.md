# core/theme

Thème Material 3 de l'app : palette (principale et états disponible / absent / non saisi /
proposé / accepté / refusé), typographie dynamique, mode sombre. Implémenté au ticket 004
à partir de `DESIGN.md`. Jusque-là, l'app utilise le thème Material 3 par défaut.
