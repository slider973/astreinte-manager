/// Hors du web : pas de barre d'adresse, rien à choisir.
///
/// Cette version est compilée pour les builds iOS et Android natifs, qui ne
/// sont produits qu'à la demande d'une caserne (`CLAUDE.md`). Elle existe pour
/// que `main()` n'ait pas à savoir où il tourne.
void adopterAdressesSansDiese() {}
