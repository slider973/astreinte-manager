# Analyse statique du projet Astreinte SP.
# Base : flutter_lints, renforcée par des règles supplémentaires et le mode strict
# du typage. `flutter analyze` doit être vierge avant toute PR.

include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "build/**"

linter:
  rules:
    # Typage et déclarations
    - always_declare_return_types
    - avoid_dynamic_calls
    - avoid_type_to_string
    - omit_local_variable_types
    - prefer_final_locals
    - prefer_final_in_for_each
    - type_annotate_public_apis

    # Immutabilité et const
    - prefer_const_constructors
    - prefer_const_constructors_in_immutables
    - prefer_const_declarations
    - prefer_const_literals_to_create_immutables
    - use_named_constants

    # Asynchrone
    - unawaited_futures
    - avoid_void_async
    - unnecessary_await_in_return
    - cancel_subscriptions
    - close_sinks
    - discarded_futures

    # Erreurs
    - only_throw_errors
    - throw_in_finally
    - test_types_in_equals
    - avoid_catches_without_on_clauses

    # Style et lisibilité
    - directives_ordering
    - eol_at_end_of_file
    - prefer_single_quotes
    - require_trailing_commas
    - sort_pub_dependencies
    - unnecessary_lambdas
    - unnecessary_parenthesis
    - unnecessary_statements
    - use_super_parameters
    - avoid_redundant_argument_values
    - avoid_bool_literals_in_conditional_expressions
    - avoid_unused_constructor_parameters
    - no_adjacent_strings_in_list
    - avoid_positional_boolean_parameters
    - avoid_multiple_declarations_per_line

    # Flutter
    - use_colored_box
    - use_decorated_box
    - sized_box_shrink_expand
    - use_key_in_widget_constructors
    - avoid_unnecessary_containers
